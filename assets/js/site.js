const toggle=document.querySelector('.menu-button');const nav=document.querySelector('.nav-links');if(toggle&&nav){toggle.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}const filterButtons=document.querySelectorAll('[data-filter]');const publications=document.querySelectorAll('[data-type]');const search=document.querySelector('#publication-search');function applyPublicationFilters(){const active=document.querySelector('[data-filter].active')?.dataset.filter||'All';const q=(search?.value||'').trim().toLowerCase();publications.forEach(pub=>{const typeOk=active==='All'||pub.dataset.type===active;const textOk=!q||pub.innerText.toLowerCase().includes(q);pub.hidden=!(typeOk&&textOk)})}filterButtons.forEach(btn=>btn.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.remove('active'));btn.classList.add('active');applyPublicationFilters()}));search?.addEventListener('input',applyPublicationFilters);
